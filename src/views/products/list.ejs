<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head") %>
</head>
<body>
    <div class="todo">
        <%- include("../partials/header") %>

        <a href=""><article class="banner">
            
        </article></a>
        <div class="products">
            <% homeProductos.forEach(product => { %>
                <article class="product">
                    <a href="/productos/<%= product.id %>"><img src="<%= product.image %>" alt=""></a>
                    <p>$ <%= product.precio %></p>
                    <a href="/productos/<%= product.id %>"><%= product.nombre %></a>
                </article>
                <% }) %>
        </div>
    </div>
    <footer>
        <%- include("../partials/footer") %>
    </footer>
</body>
</html>